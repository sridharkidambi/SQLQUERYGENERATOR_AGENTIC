# Core pipeline (ingestion_pipeline.py, agent_orchestrator.py)
psycopg2-binary>=2.9
sentence-transformers>=2.7
networkx>=3.2
sqlglot>=25.0
anthropic>=0.34

# Evaluation harness (evaluate.py) — see EVALUATION.md for why DeepEval
# is primary here rather than ragas (a real dependency conflict was hit
# pulling ragas into this same environment).
deepeval>=1.0

# Optional — only if you isolate ragas per EVALUATION.md's guidance
# rather than installing it alongside the above in this same image:
# ragas
